import { styled } from '@mui/material/styles';

import Typography from '@mui/material/Typography';

const StyledTourDescription = styled(Typography)(({ theme }) => ({
	display: 'inline',
	fontSize: 15,
	fontWeight: 500,
}));

export default StyledTourDescription;
