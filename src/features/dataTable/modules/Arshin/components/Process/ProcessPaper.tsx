import { styled } from '@mui/material';
import Paper from '@mui/material/Paper';

export const ProcessPaper = styled(Paper)(({ theme }) => ({
	display: 'flex',
	flexDirection: 'column',
	width: 300,
}));
