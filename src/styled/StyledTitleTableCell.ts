import styled from '@emotion/styled';
import { TableCell } from '@mui/material';

const StyledTitleTableCell = styled(TableCell)({
	fontWeight: 'bold',
	border: '2px solid #000',
	padding: 0,
	textAlign: 'center',
});

export default StyledTitleTableCell;
